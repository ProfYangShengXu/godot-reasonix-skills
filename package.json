{
  "name": "godot-reasonix-skills",
  "version": "2.0.0",
  "description": "🎮 Godot Reasonix Skills — AI Agent 操控 Godot 引擎的工具集，含 M1-M11 全套接口 + Bobanana 4.0 管线集成 + 调试闭环",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:u": "vitest run tests/u/",
    "test:i": "vitest run tests/i/",
    "test:s": "vitest run tests/s/",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
